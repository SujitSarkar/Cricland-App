import 'package:flutter/material.dart';

class IPLPage extends StatelessWidget {
  const IPLPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('IPL Page')),
    );
  }
}
